# =============================================================================
# Crypto Mecca Wallet - Docker Compose
# =============================================================================

services:
  crypto-mecca:
    container_name: crypto-mecca-wallet
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "80:80"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - crypto-network

networks:
  crypto-network:
    driver: bridge